name: Build website
on:
  push:
    branches:
    - master

jobs:
  build:
    runs-on: silex/emacs
    steps:
    - uses: actions/checkout@master
      with:
        fetch-depth: 1
    - name: build
      run: |
        mkdir -p docs
        emacs --batch -l publish.el --eval '(publish-site "docs" t)'
